# Helpful aliases
alias cls='clear' # clear terminal
alias  l='eza -lh  --icons=auto' # long list
alias ls='eza -1   --icons=auto' # short list
alias ll='eza -lha --icons=auto --sort=name --group-directories-first' # long list all
alias ld='eza -lhD --icons=auto' # long list dirs
alias la='eza -la  --icons=auto' # hidden list

# package manager aliases
alias remove='$aurhelper -Rns' # uninstall package
alias update='sudo pacman -Syu --noconfirm && $aurhelper -Syu --noconfirm && flatpak update -y' # update system/package/aur and flatpak apps
alias install='$aurhelper -S --needed' # install new package
alias search='$aurhelper -Ss' # list availabe package
alias list='$aurhelper -Qs' # list installed package

alias pc='$aurhelper -Sc' # remove unused cache
alias po='sudo pacman -Qtdq | sudo pacman -Rns -' # remove unused packages, also try > sudo pacman -Qqd | sudo pacman -Rsu --print -

alias code='code --ozone-platform-hint=wayland --disable-gpu' # gui code editor
alias brave='brave --enable-chrome-browser-cloud-management'  # gui web browser
alias vim='nvim' # neovim

# flatpak aliases
alias fl='flatpak list'
alias fu='flatpak update -y'
alias fi='flatpak install -y'
alias frn='flatpak run'
alias frm='flatpak uninstall'
alias fs='flatpak search'

# git aliases
alias gs='git status'
alias ga='git add .'
alias pull='git pull'
alias push='git push'
alias commit='git commit -m'
alias clone='git clone'

# lenovo drivers aliases
alias lenovo='sudo ~/src/LenovoLegionLinux/python/legion_linux/legion_linux/legion_gui.py'

# Handy change dir shortcuts
alias ..='cd ..'
alias ...='cd ../..'
alias .3='cd ../../..'
alias .4='cd ../../../..'
alias .5='cd ../../../../..'

# Always mkdir a path (this doesn't inhibit functionality to make a single dir)
alias mkdir='mkdir -p'

# Fixes "Error opening terminal: xterm-kitty" when using the default kitty term to open some programs through ssh
alias ssh='kitten ssh'

